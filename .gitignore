# secrets — never commit
.env
.env.local
.env.*.local

# deps
node_modules/

# local run outputs (committed reference outputs live directly in results/, not results/local/)
results/local/

# misc
*.log
.DS_Store

# internal R&D scratch — kept local, not for the public repo
experiments/
results-recipient.txt
